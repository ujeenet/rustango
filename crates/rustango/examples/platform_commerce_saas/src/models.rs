//! Project-level models.
//!
//! Empty on purpose: this app's models all live in the `commerce` app
//! module (`src/commerce/models.rs`), which is where `manage startapp`
//! puts them and where the admin registry finds them either way. The
//! generator's starter `Item` model was deleted rather than left to
//! appear in every migration and every admin index.
