/target
# Cargo.lock IS committed. This workspace ships a binary
# (`cargo-rustango`), which is the textbook case for committing it, and
# ignoring it cost us twice: a fresh CI resolve picked up the broken
# `time` 0.3.48 and then `tinyvec` 1.13.0, reddening every job on a
# green commit with no change of ours. Committing it makes CI
# reproducible and lets a bad upstream release be handled deliberately
# (bump the lock when we choose) instead of at the moment it lands.
**/*.rs.bk

# Nested example projects (cookbook_blog, blog_demo, etc.) are
# standalone Cargo packages with their own target/ + Cargo.lock —
# don't commit either.
crates/rustango/examples/*/target/
# …but their Cargo.lock IS committed, for the same reason as the
# workspace one above. These packages sit outside `members`, so they
# resolve their own dependency tree — the workspace lock does not reach
# them, and `tinyvec` 1.13.0 reddened every `doc_examples` job even
# after the workspace lock was pinned.
# shared target dir bin/examples.sh builds every example into (override: EX_TARGET)
/target-examples/
.DS_Store
.idea/
.vscode/

# AI / coding-agent tooling — per-developer local config, never shared.
# These files accumulate absolute paths, per-machine command allowlists,
# and session state; they are not part of the project.
#
# `.claude/skills/` is the exception: a skill is project knowledge — the
# same thing a CONTRIBUTING section would be, addressed to an agent — and
# carries no machine-local state. Track those; ignore the rest.
# (Listing `.claude/*` rather than `.claude/` matters: git does not descend
# into an excluded directory, so a negation under `.claude/` would never
# be reached.)
.claude/*
!.claude/skills/

# …but only skills that are *this* project's knowledge. The `review-*`
# crew-review skills belong to a PHP/Vue codebase — they talk about
# PHPUnit, Quasar, Semantic UI, Elasticsearch and `ClientTestCase`, none
# of which exist here — and arrived by way of a shared agent config.
# Tracking them would put another project's instructions in rustango's
# history, where they would be read as guidance for this one.
#
# A rustango-owned review skill is welcome; it needs a name that is not
# `review-*`, or an explicit negation below.
.claude/skills/review-*/
CLAUDE.md
AGENTS.md
.mcp.json
.cursor/
.cursorrules
.cursorignore
.cursorindexingignore
.aider*
.continue/
.windsurf/
.windsurfrules
.codeium/
.roo/
.roomodes
.cline/
.clinerules
.gemini/
GEMINI.md
.junie/
.qodo/
.specstory/
.github/copilot-instructions.md
.github/instructions/
.github/prompts/

# manage_demo example writes its migrations here at runtime
/manage_demo_migrations/
# tenancy_manage example writes packaged bootstrap migrations here on first run
/tenancy_manage_migrations/
# local env file with DATABASE_URL etc — never commit
.env
# playwright-mcp debug output (screenshots, page snapshots, console logs)
.playwright-mcp/

# SQLite app demo runtime artifacts
/var/


# Persisted session secrets (#69) — dev fallback when RUSTANGO_SESSION_SECRET unset
.rustango_*_session.key
